import React  from 'react'
import Header from './Header'
const FlashCards = (props) => {

    return (
        <div>
            Thi is flashcard page
            <Header />
        </div>
    )
}

export default FlashCards
